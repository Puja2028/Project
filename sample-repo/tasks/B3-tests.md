# B3 — Test Discovery and Execution (15 min)

**Goal:** Find the test framework, relevant test files, and exact commands.

## Required output

1. **Test framework and config file** — e.g. pytest, `pytest.ini`
2. **Relevant test files** — list paths
3. **Exact commands** — copy-paste ready
4. **Actual command result** — paste terminal output
5. **Any failure and interpretation** — if tests fail, explain why

## Expected commands

```bash
cd sample-repo
pip install -r requirements.txt
pytest -v
```

## Self-check

Can you do this in 15 minutes?
